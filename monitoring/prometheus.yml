global:
  scrape_interval: 15s # How often to check your API

# This is where you link the alerts!
rule_files:
  - "alerts.yml" 

scrape_configs:
  - job_name: "forensic_audit_api"
    static_configs:
      - targets: ["192.168.1.100:8001"] # Points to your FastAPI